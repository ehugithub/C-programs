#include <stdio.h>
#include <stdlib.h>

struct node () {
	int data;
	struct node *left, *right;
	
} start* = NULL;

void insert(struct node *, int);
void delete(struct node *);


int main () {

	return 0;
}
